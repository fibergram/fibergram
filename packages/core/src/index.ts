/**
 * `@fibergram/core` — Dialog/decider, receive, Decision, EntityManager, Router, typed errors, CallbackData
 *
 * Package skeleton: the public API lands incrementally through M0 (see docs-ai/fibergram-design.md).
 *
 * @since 0.0.0
 */

/**
 * Skeleton package version. Placeholder until the first real export.
 *
 * @example
 * import { version } from "@fibergram/core"
 *
 * console.log(version)
 *
 * @category constants
 * @since 0.0.0
 */
export const version = "0.0.0" as const
