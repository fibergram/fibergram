# The Effect ecosystem uses strict peer deps, but the beta perimeter is volatile —
# don't fail on peer mismatch, just warn.
strict-peer-dependencies=false
auto-install-peers=true

# Deterministic installs in CI and locally.
save-exact=false
prefer-workspace-packages=true
link-workspace-packages=true
